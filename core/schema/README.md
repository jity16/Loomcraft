# Wire schemas

These JSON Schemas document the cross-language wire contracts. They are
intentionally descriptive; the Python validator additionally enforces graph
acyclicity, registry authorization, revision monotonicity, and state-machine
rules that JSON Schema cannot express.
