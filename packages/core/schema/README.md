# Wire schemas

These JSON Schemas document the public cross-language contracts for Plan, Event
and Agent tools. They are intentionally descriptive; the Python validator also
enforces graph acyclicity, registry authorization, revision monotonicity and
state-machine rules that JSON Schema cannot express.
